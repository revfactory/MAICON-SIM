# MAICON 경기장 시뮬레이터

국방 AI 경진대회 본선 경기장(5 m × 3.5 m)을 Blender MCP로 재현하고, UGV 주행 데모 영상을 산출하는 프로젝트.

## 하네스: MAICON 시뮬레이터

**목표:** 도면 판독부터 1080p 데모 영상 렌더까지, 경기장 3D 재현 전 과정을 에이전트 팀으로 자동화한다.

**트리거:** 경기장·트랙·시뮬레이션·데모 영상·Blender 관련 작업 요청 시 `maicon-sim-orchestrator` 스킬을 사용하라. 부분 수정("건물만 다시", "카메라 바꿔줘")과 재실행 요청도 동일하다. 단순 질문은 직접 응답 가능.

**핵심 제약:** Blender MCP는 단일 가변 상태다. 빌드 에이전트는 스크립트 파일만 작성하고, 실행은 오케스트레이터가 순차로 한다. 이 규약을 깨면 씬 충돌을 추적할 수 없다.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-01 | 초기 구성 (harness v2) | 전체 | 에이전트 7 + 스킬 7 |
| 2026-08-02 | 첫 전체 실행 후 보강 | 오케스트레이터, blender-mcp-protocol, scene-qa, verify_scene.py, motion-director, scene-dresser, ugv-motion | 실전에서 드러난 함정 반영: 빌드 에이전트에 **실측값을 제공**하지 않으면 기하 추론으로 스톨(3회 발생) / `exec` 네임스페이스 비유지 / 장시간 렌더의 MCP 타임아웃은 실패가 아님 / 프레이밍 판단에 뷰포트 스크린샷 금지 / 검수 대조 기준 `_Base` / `ammo_box` 명명 충돌 / 곡률 목표는 체크포인트보다 후순위 / `forward_axis` 하드코딩 금지 |
| 2026-08-02 | 환경 모듈 추가 | `85_environment.py` | 실내 경기장(바닥·벽·천장·조명) — 검은 허공 배경은 스케일감을 죽인다 |
| 2026-08-02 | 실제 과제 명세 반영 (v2) | `75_drone.py`, `82_drone_motion.py` 신규 / `40`·`55`·`60`·`80`·`90` 수정 | 참가자 후기로 확인한 본선 과제: 화재 건물 2동(목표추적), 드론 정찰, QR 아군표식, 객체 24곳, 장애물 회피 강화. 컷 11→13, 카메라 4→7 |
| 2026-08-02 | 오케스트레이터 오판 대응 규칙 추가 | 오케스트레이터, track-builder | 도면 판독은 오케스트레이터만 가능하지만 그것이 옳다는 보장은 없다. 에이전트가 FAIL을 보고하면 지시를 관철하지 말고 근거를 먼저 검토한다. 실제로 차선 이중선화 오판을 에이전트 검산이 잡았다 |
